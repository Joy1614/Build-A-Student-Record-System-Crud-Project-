# Build-A-Student-Record-System-Crud-Project-
This project is a simple student record management system built using Python. It helps in adding, viewing, updating, and deleting student details. The data is stored using files like JSON or CSV, and proper input validation is done to avoid errors. It makes record handling easy and organized.
